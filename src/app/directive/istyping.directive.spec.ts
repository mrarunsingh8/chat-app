import { IstypingDirective } from './istyping.directive';

describe('IstypingDirective', () => {
  it('should create an instance', () => {
    const directive = new IstypingDirective();
    expect(directive).toBeTruthy();
  });
});
